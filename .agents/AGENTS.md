# Project Rules and Behavioral Directives

## Preserving User Settings and Form Configurations
- NEVER change, alter, or revert any previously configured forms, UI layouts, settings, or database structures unless explicitly requested by the USER.
- All previously implemented features, modal popups, data grid alignments, and user options must be strictly preserved across all future updates.
